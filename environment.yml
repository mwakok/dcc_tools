name: ghproject
channels:
  - conda-forge
  - defaults
dependencies:
  - black
  - bump2version
  - notebook
  - pip
  - pre_commit
  - pytest
  - pytest-cov
  - python >=3.7,<3.9
  - requests>=2.24.0
  - pip:
      - python-frontmatter
